package cmd

// Cobra auto-registers the `completion` subcommand on every root command,
// providing `tok completion bash|zsh|fish|powershell` for free.
//
// This file is intentionally a placeholder so the directory layout from
// plan §3 is honored; no custom completion logic is needed.
